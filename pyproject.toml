[build-system]
requires = ["setuptools>=61.0", "wheel", "pip>=26.0,<27.0.0", "uv>=0.9.25,<1.0.0"]
build-backend = "setuptools.build_meta"

[project.urls]
Homepage = "https://github.com/TECHLETES/python_basic_template"
Repository = "https://github.com/TECHLETES/python_basic_template"
Issues = "https://github.com/TECHLETES/python_basic_template/issues"

[project]
name = "python-basic-template"
version = "0.1.0"
description = "A basic Python project template with full DevOps tooling support."
readme = "README.md"
requires-python = ">=3.12,<4.0"
keywords = ["devops", "template", "python"]
authors = [{ name = "Thom Ezechiels", email = "thom@techletes.ai" }]
maintainers = [{ name = "Techletes DevOps Team", email = "dev@techletes.ai" }]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
]

# Core production dependencies required for the application to run.
# Pinned with >= and < to allow patch/minor updates while preventing major version breaks.
# Add only dependencies that are needed for the app to run in production.
dependencies = [
  "flask>=3.1.3,<4.0.0",     # Example dependency, replace with your own
  "requests>=2.32.0,<3.0.0",
  "urllib3>=2.6.0,<3.0.0",
  "werkzeug>=3.1.6,<4.0.0",
  "filelock>=3.20.0,<4.0.0",
]

# Optional dependency groups for different environments and use cases.
# Managed by uv (PEP 735 format) and locked in uv.lock for reproducible builds.
# Install with: uv sync (all groups), uv sync --no-group dev (no dev), uv sync --only-group extra (extra only), etc.
[dependency-groups]
# Development and testing dependencies.
# Includes code quality tools, testing frameworks, and development utilities.
# Install with: uv sync (default includes dev) or uv sync --no-group dev to skip
dev = [
  "pre-commit>=4.5.0,<5.0.0",
  "detect-secrets>=1.5.0,<2.0.0",
  "nbstripout>=0.8.0,<1.0.0",
  "ipykernel>=6.29.0,<7.0.0",
  "pytest>=9.0.3,<10.0.0",
  "pytest-cov>=7.0.0,<8.0.0",
  "black>=25.12.0,<27.0.0",
  "mypy>=1.19.0,<2.0.0",
  "ruff>=0.14.0,<1.0.0",
  "beartype>=0.22.0,<1.0.0",
  "pytest-beartype>=0.2.0,<1.0.0",
  "pip-audit>=2.10.0,<3.0.0",
  "rich>=14.2.0,<15.0.0",
  "pyupgrade>=3.15.0,<4.0.0",
  "codespell>=2.2.0,<3.0.0",
  "bandit>=1.7.0,<2.0.0",
  "pydocstyle>=6.3.0,<7.0.0",
  "genbadge[coverage]>=1.1.0,<2.0.0",
  "testcontainers>=4.8.0",
  "pillow~=12.2"
]

# Additional production features and optional components.
# Install with: uv sync --group extra or uv sync --no-group extra
extra = [
  # "zero-friction @ git+https://github.com/TECHLETES/zero_friction.git@main"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["utils*", "example*"]
exclude = ["tests*"]

[tool.black]
line-length = 88
target-version = ["py312"]

[tool.pytest]
minversion = "9.0.3"
addopts = [
  "--strict-markers",
  "--strict-config",
  "--cov=utils",
  "--cov=example",
  "--cov-report=term-missing",
  "--cov-report=html",
  "--cov-report=xml",
  "--beartype-packages=utils,example",
]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
pythonpath = ["."]

[tool.coverage.run]
source = ["utils", "example"]
omit = [
  "*/__pycache__/*",
  "*/tests/*",
  "*/example/*",
  "*/.venv/*",
  "*/venv/*",
  "*/__init__.py",
  "setup.py",
  # Add any other files you want to exclude from test coverage
]

[tool.coverage.report]
exclude_lines = [
  "pragma: no cover",
  "def __repr__",
  "if self.debug:",
  "if settings.DEBUG",
  "raise AssertionError",
  "raise NotImplementedError",
  "if 0:",
  "if __name__ == .__main__.:",
  "class .*\\bProtocol\\):",
  "@(abc\\.)?abstractmethod",
  "except ImportError",
  "pass",
]

[tool.mypy]
python_version = "3.12"

# ignore_missing_imports for third-party libs without type hints
ignore_missing_imports = true
follow_imports = "silent"

# strictness settings - adjust as needed
warn_unused_ignores = true
warn_return_any = true
warn_unreachable = true
no_implicit_optional = true
allow_redefinition = true
show_column_numbers = true
pretty = true
warn_unused_configs = true

# enforce typing on own code
disallow_untyped_defs = true
disallow_incomplete_defs = true

# Specify which folders/files to type check (adjust as needed)
files = ["example"]

exclude = '''
(?x)(
    ^migrations/
  | ^notebooks/
  | ^tests/
  | ^\.venv/
  | ^venv/
)
'''
plugins = []

[tool.ruff]
target-version = "py312"
line-length = 88
fix = true
respect-gitignore = true
extend-exclude = ["migrations"]
lint.dummy-variable-rgx = "^_|^__|^pytest$"

# Enable popular rule sets
lint.select = [
  "E",  # pycodestyle
  "F",  # pyflakes
  "I",  # import sorting
  "B",  # bugbear
  "UP", # pyupgrade
]

# Rules to ignore (common ones)
lint.ignore = [
  "B008", # Do not perform function calls in argument defaults
  "E501", # Line too long (Black handles this)
]

[tool.bandit]
exclude_dirs = ["tests", ".venv"]
skips = ["B101", "B601"]

[tool.pip-audit]
# Centralized CVE/PYSEC exclusions for pip-audit
# Used by both pre-commit hooks and CI workflows via scripts/hooks/run-pip-audit.sh
# See docs/6_pre_commit_troubleshooting.md for explanation of each exclusion
ignore = [
  "CVE-2026-1703", # Path traversal in pip when extracting malicious wheels
]

[tool.uv]
required-version = ">=0.9.25,<1.0.0"
